# matzip PRD (Product Requirements Document)

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 이름 | matzip |
| 한 줄 설명 | "가볼 곳"을 담아두고, 방문 후 기록하며, "오늘 뭐 먹지" 고민을 해결해주는 지도 기반 맛집 관리 웹 서비스 |
| 유형 | 웹 서비스 |
| 난이도 | 중급 (지도 API, 상태 관리 로직, 조건 기반 추천, 필터링/검색 포함) |

---

## 2. 사용자 시나리오

- **누가**: 회원가입/로그인한 사용자 (개인별 데이터로 관리)
- **언제/왜**:
  1. 지도를 둘러보다 마음에 드는 곳을 발견하면 **"가볼 곳"**으로 담아둔다 (F1)
  2. 실제로 방문한 후 재방문 의사와 짧은 메모를 남겨 **"가본 곳"**으로 전환한다 (F2)
  3. 오늘 뭘 먹을지 고민될 때, 조건(카테고리·태그)을 입력해 **"오늘 뭐 먹지"** 추천을 받아 결정한다 (F3, 핵심 가치)
  4. 맛집 상세에서 다른 사용자들의 방문기록을 검색/필터링해보고, AI가 요약·감성분석해준 정보로 빠르게 판단한다
  5. 대시보드에서 내가 담은 맛집의 카테고리 비율, 가볼곳/가본곳 현황 등을 시각적으로 확인한다

---

## 3. 핵심 기능 목록

### 필수 기능 (MVP)
| 기능 | 설명 |
|---|---|
| F1. 맛집 담기 | 맛집을 **"가볼 곳"** 상태로 등록. 서비스의 진입점 |
| F2. 상태 전환 + 방문기록 | "가볼 곳" → "가본 곳" 전환 시, 재방문 의사 중심의 짧은 방문기록 작성 |
| F3. 오늘 뭐 먹지 | 조건을 입력하면 목록에서 후보를 골라줌. **이 서비스의 핵심 가치** |
| 지도 탐색 | 지도에서 맛집 위치 표시/탐색 |
| 검색 기능 | 카카오맵 전체 장소 대상 상호명 검색 |
| 로그인/회원가입 | Supabase Auth 기반 인증, 개인별 데이터 관리 |
| 방문기록 리스트 검색/필터 | 맛집 상세의 방문기록들을 클라이언트에서 JSON으로 받아 검색/필터링 |
| AI 리뷰 요약 | 하나의 맛집에 쌓인 여러 방문기록(한줄메모)을 Gemini API로 한 줄 요약 |
| AI 감성분석 | 방문기록 한줄메모를 긍정/부정으로 분류해 맛집 상세에 비율로 표시 (Gemini API) |
| 대시보드 | 도넛차트(카테고리 비율, 가볼곳/가본곳 비율) + 워드클라우드(맛집별 방문기록 자주 나온 단어) |

### 보조 기능
| 기능 | 설명 |
|---|---|
| F4. 태그 분류 | 혼밥/회식/가성비 등, F3(오늘 뭐 먹지)의 조건으로 활용되는 분류 기준 |

### 선택 기능 (향후 추가)
- 방문기록에 좋아요/도움이되었어요 반응
- 내 주변 위치 기반 맛집 추천

> **참고**: 기존에 계획했던 별점 리뷰, 리뷰 사진 첨부, 리뷰 최신순 정렬 기능은 F2(방문기록)로 대체되며 이번 개편으로 제외되었습니다. 또한 이번 업데이트로 "로그인 없이 이용"이라는 이전 방향이 변경되어, 이제 회원가입/로그인 후 개인별 데이터로 관리됩니다.

---

## 4. 기술 스택

| 영역 | 기술 |
|---|---|
| 프레임워크 | Next.js |
| DB/백엔드 | Supabase |
| 인증 | Supabase Auth |
| 배포 | Vercel |
| 지도 API | 카카오맵 API |
| AI API | Google Gemini API (리뷰 요약, 감성분석) |
| 스타일링 | Tailwind CSS |
| 상태관리 | 기본 React 상태관리 (useState/useEffect) |
| 데이터 시각화 | 도넛차트/워드클라우드 라이브러리 (예: recharts, react-wordcloud 등) |

---

## 5. 화면 구성

- **페이지 구조**: 메인 화면(지도+리스트) + **대시보드 페이지** (별도 페이지 2개 구조)
- **레이아웃(메인)**: 지도와 리스트를 좌/우 반반 비중으로 배치
- **리스트 필터**: 리스트 상단에 **전체 / 가볼 곳 / 가본 곳** 필터 버튼으로 전환
- **"오늘 뭐 먹지" 진입점**: 화면 상단에 항상 노출되는 플로팅 버튼 (어느 화면에서든 바로 접근 가능)
- **모달 처리**: 아래 기능은 모두 모달(팝업)로 표시
  - 로그인/회원가입 모달
  - 맛집 담기 모달 (F1: 이름/주소/카테고리 검색 + 태그 지정)
  - 맛집 상세 모달 (현재 상태 확인, "가본 곳"으로 전환 버튼, 방문기록 검색/필터, AI 요약·감성분석 결과 표시)
  - 방문기록 작성 모달 (F2: 재방문 의사 토글 + 한줄 메모 + 태그 수정)
  - 오늘 뭐 먹지 모달 (F3: 조건 선택 → 조건에 맞는 후보 목록 표시 → 사용자가 선택)
- **대시보드 페이지**: 도넛차트(카테고리 비율, 가볼곳/가본곳 비율) + 워드클라우드(맛집별 방문기록 자주 나온 단어)

---

## 6. 상세 기능 명세

### 6.1 F1. 맛집 담기
- **입력 항목**: 이름, 주소, 카테고리, 태그(선택)
- **이름/주소**: 카카오 장소검색 API로 검색 후 선택 → 주소·좌표 자동 입력
- **카테고리**: 사전 정의된 목록에서 선택 (한식/중식/일식/양식/카페 등)
- **태그**: F4에서 정의한 태그 목록 중 선택 (선택 사항, 나중에 F2에서도 수정 가능)
- **결과**: 담은 즉시 **"가볼 곳"** 상태로 등록됨

### 6.2 F2. 상태 전환 + 방문기록
- **트리거**: 맛집 상세 모달에서 "가본 곳으로 전환" 액션
- **입력 항목**: 재방문 의사(O/X, 토글) + 한줄 메모(텍스트)
- **태그 수정**: 방문기록 작성 시 태그도 함께 수정 가능
- **결과**: 상태가 "가볼 곳" → "가본 곳"으로 변경되고, 방문기록이 저장됨

### 6.3 F3. 오늘 뭐 먹지 (핵심 기능)
- **조건 입력**:
  - 대상 상태: 가볼 곳 / 가본 곳 / 둘 다 (사용자가 선택)
  - 카테고리 (선택)
  - 태그 (선택)
- **결과 표시**: 조건에 맞는 후보를 **여러 개 목록으로 보여주고**, 그중 사용자가 직접 하나를 고름
- **예외 처리**: 조건에 맞는 곳이 없을 경우 안내 문구 표시 (조건 완화 유도)

### 6.4 F4. 태그 분류
- **역할**: F3(오늘 뭐 먹지)의 필터 조건으로 사용되는 분류 기준
- **예시 태그**: 혼밥, 회식, 가성비 등
- **부여 시점**: F1(담기)에서 최초 지정, F2(방문기록)에서 수정 가능
- **형태**: 사전 정의된 태그 목록에서 다중 선택 (MVP 기준)

### 6.5 리스트 필터 & 검색
- **리스트 필터**: 전체 / 가볼 곳 / 가본 곳 버튼으로 리스트 전환
- **검색**: 카카오맵 전체 장소 대상 검색
  - 우리 DB에 등록된 곳 → 클릭 시 상세 모달 표시
  - 미등록 장소 → 클릭 시 맛집 담기(F1) 폼으로 바로 연결

### 6.6 로그인/회원가입
- **인증 방식**: Supabase Auth (이메일 기반 또는 소셜 로그인, 세부 방식은 개발 단계에서 결정)
- **데이터 범위**: 로그인한 사용자별로 "가볼 곳/가본 곳" 목록, 방문기록, 태그가 개인 데이터로 관리됨
- **비로그인 접근**: 로그인 전에는 맛집 탐색/검색까지는 가능하되, 담기(F1)·방문기록(F2) 등 쓰기 기능은 로그인 필요 *(세부 정책은 개발 단계에서 조정 가능)*

### 6.7 방문기록 리스트 검색/필터
- **데이터 소스**: Supabase에서 받아온 방문기록 데이터를 클라이언트에서 JSON으로 파싱
- **처리 방식**: 클라이언트 사이드에서 검색어/필터 조건에 따라 필터링 (서버 재요청 없이 즉시 반응)
- **필터 기준**: 태그, 재방문 의사(O/X) 등

### 6.8 AI 리뷰 요약 (Gemini API)
- **대상**: 하나의 맛집에 대해 여러 사용자가 남긴 방문기록(한줄메모)
- **동작**: 방문기록 텍스트들을 모아 Gemini API에 전달 → 한 줄 요약 결과를 맛집 상세에 표시
- **트리거 시점**: 맛집 상세 조회 시 (또는 방문기록이 일정 개수 이상 쌓였을 때, 캐싱 방식은 개발 단계에서 결정)

### 6.9 AI 감성분석 (Gemini API)
- **대상**: 방문기록의 한줄메모 텍스트
- **동작**: 각 메모를 긍정/부정으로 분류 → 맛집 상세에 비율로 표시 (예: "긍정 80%")
- **활용**: 사용자가 방문기록을 일일이 읽지 않아도 전반적인 반응을 빠르게 파악 가능

### 6.10 대시보드
- **위치**: 별도 페이지
- **도넛차트**:
  - 내가 담은 맛집의 카테고리 비율 (한식/중식/카페 등)
  - 가볼 곳 vs 가본 곳 비율
- **워드클라우드**: 특정 맛집의 여러 사용자 방문기록에서 자주 나온 단어 시각화

---

## 7. 디자인 가이드

| 항목 | 내용 |
|---|---|
| 무드/톤 | 미니멀/깨끔한 느낌 (흰색 바탕, 여백 많이) |
| 포인트 컬러 | 오렌지/코럴 |
| 폰트 | Pretendard |

---

## 8. 제약사항

- **예산/비용**: 완전 무료 티어만 사용 (Supabase / Vercel / 카카오맵 / Gemini API 무료 토큰량·요청량 내에서)
- **개발 기간**: 1~2주 내 빠르게 완성
- **스팸/도배 방지**: MVP에서는 신경 쓰지 않음 (학습용이므로 실제 운영 이슈는 추후 고려)
- **지원 환경**: 반응형(모바일/데스크톱 둘 다) 기본 지원
- **AI API 사용량**: Gemini API 무료 티어의 요청 제한(rate limit)을 고려해, AI 요약·감성분석은 캐싱하거나 호출 빈도를 제한하는 방식 검토 필요
